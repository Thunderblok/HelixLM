# Helix Width-First and Vertical-Depth-3 Experiment V0

## Decision

`d_model=768` has priority over adding columns or raising `vertical_depth`.
Vertical depth 3 remains a real follow-on experiment, but only after the
width-first candidate passes its numerical, memory, throughput, and quality
courts.

This packet does not start training and does not use the GPU.

## Reference

The recipe and quality anchor is MLflow run
`8ad467637b8b407aaa1c5ced49d6384f`:

```text
d_model=512
n_heads=8
n_columns=3
lateral_p=0.5
vertical_p=0.7
vertical_depth=2
ffn_expansion=3.0
lr=2e-4
seq_len=512
loops=3
parameters=54,771,988
best_validation_ppl=45.0672
```

Its resolved-configuration SHA-256 is
`9ae8b80ee1eaabd587491a82c4a42e18d654a3d1c194a41722de7ad25a253ea0`.

## Structural findings

With three columns, `vertical_depth=3` is vacuous. The greatest possible
predecessor-column distance is two, and the generated graph root is identical
to the depth-2 graph root.

A depth-3 graph therefore requires at least four columns. The current graph
executor also creates learned merge layers whose input width depends on the
number of predecessors. Raising depth can change parameter count as well as
connectivity; it must not be described as an identical-parameter ablation.

CPU-only preflight results at seed 42:

| Treatment | Parameters | Nodes | Edges | Maximum observed vertical reach |
| --- | ---: | ---: | ---: | ---: |
| 512d, 3 columns, depth 2 | 54,771,988 | 9 | 22 | 2 |
| 512d, 3 columns, depth 3 | 54,771,988 | 9 | 22 | 2 |
| 512d, 4 columns, depth 2 | 65,675,290 | 12 | 37 | 2 |
| 512d, 4 columns, depth 3 | 66,461,722 | 12 | 36 | 3 |
| 768d, 3 columns, depth 2 | 103,883,156 | 9 | 22 | 2 |
| 768d, 4 columns, depth 2 | 128,397,338 | 12 | 37 | 2 |
| 768d, 4 columns, depth 3 | 130,166,810 | 12 | 36 | 3 |

At width 768, the fourth column costs 24,514,182 parameters before depth is
raised. Moving that four-column graph from depth 2 to depth 3 adds another
1,769,472 parameters at this seed because predecessor-dependent merge layers
change width.

The shared seed does not create a coupled graph pair. Adding depth-3 candidate
edges changes the random-number draw order, and the observed four-column
depth-2/depth-3 graphs contain 37 and 36 edges respectively. Before a quality
ablation, freeze an explicit topology manifest or key each random decision by
edge identity. The candidate must differ from its control only by admitted
distance-3 edges; seed equality alone is not sufficient.

The 768-wide treatments use 12 heads to preserve the reference 64-element
attention-head width. That is a preflight choice and must be reconciled with
David's final production configuration before a full run.

## Admission sequence

1. Finish the currently active exploration without interruption.
2. Run a bounded strict-BF16 5080 smoke for the 768-wide, three-column,
   depth-2 candidate.
3. If memory, throughput, finite-gradient, and checkpoint courts pass, run the
   width-first matched pilot and compare it with the frozen evaluator.
4. Freeze a coupled four-column topology pair whose common edges are identical
   and whose candidate-only edges have predecessor distance three.
5. Only after width-first admission, run bounded four-column depth-2 and
   depth-3 topology pilots from identical source, topology manifest, corpus
   order, optimizer, evaluator, and stopping rule.
6. A full-corpus depth-3 run requires a positive pilot terminal; it is not
   implied by the structural preflight.

The probability treatment (`lateral_p` and `vertical_p`) remains a separate
ablation. It must not be combined with the first depth comparison.

## MLflow contract

Every topology run records at least:

```text
source_head
source_tree
reference_run_id
d_model
n_heads
n_columns
nodes_per_column
lateral_p
vertical_p
vertical_depth
graph_root
node_count
edge_count
maximum_observed_predecessor_column_distance
parameter_count_total
parameter_count_trainable
seed
corpus_revision
corpus_order_root
tokenizer
seq_len
n_loops
ffn_expansion
learning_rate
```

Quality comparisons additionally require identical evaluator identity, tasks,
dataset revisions, model adapter, scoring semantics, and dependency lock.

## Evidence

The machine-readable preflight is generated by:

```bash
CUDA_VISIBLE_DEVICES="" python topology_depth3_preflight.py \
  --output /tmp/helix-topology-depth3-preflight-v0.json
```

Observed terminal root:

```text
737a4a9606aa765403006f832455b06209bee8a0c5bfb6212a2f669f9e92a3ce
```

```text
GPU_USED=false
TRAINING_STARTED=false
PRODUCTION_EFFECT=none
```
